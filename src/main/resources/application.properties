# JWT
app.jwt.secret=THIS_IS_MY_SUPER_SECRET_KEY_123456789_123456
app.jwt.access-exp-min=60
app.jwt.refresh-exp-days=7

# DATABASE (Render Compatible)
spring.datasource.url=${SPRING_DATASOURCE_URL}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD}

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# PORT (Very Important)
server.port=${PORT:8080}

# AWS
aws.region=ap-south-1
aws.bucketName=ssgroup-shop-images-shivam-2026

# Razorpay
razorpay.key=rzp_test_123456789
razorpay.secret=test_secret_123456